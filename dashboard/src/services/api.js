/**
 * API notes for the dashboard.
 *
 * Endpoints expected to be used:
 * - GET /sensor/latest
 * - GET /sensor/history
 * - POST /ai/predict-plant
 * - GET /ai/history
 * - GET /plant-profile/:plant
 * - GET /advice
 * - POST /device/control
 * - GET /system/status
 *
 * TODO:
 * - Choose fetch or axios
 * - Create reusable request helpers
 * - Handle loading/error states
 */
