/**
 * App bootstrap stub.
 * TODO:
 * - Install and configure Express
 * - Add middleware: json, cors, logger
 * - Mount route modules
 * - Add an error handler
 */

const app = {
  listen(port, callback) {
    if (callback) callback();
  }
};

module.exports = { app };
