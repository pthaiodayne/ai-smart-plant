/**
 * Device routes
 *
 * APIs to implement:
 * - POST /device/control
 * - GET /device/command
 *
 * TODO:
 * - Store the latest command for each device
 * - Optionally add an acknowledged status for ESP32
 */
